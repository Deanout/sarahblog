<p id="notice"><%= notice %></p>
<% @posts.each do |post| %>
<div>
  <h1>
  <%= link_to post.title, post %>
  </h1>
  <%= "Posted at " + post.created_at.strftime("%B, %d, %Y") %>
</div>
<div>
  <p>
    <%= post.body %>
  </p>
</div>
<% end %>
<%= link_to 'New Post', new_post_path %>